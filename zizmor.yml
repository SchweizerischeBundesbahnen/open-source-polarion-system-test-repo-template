---
rules:
  # ci.yml: SONAR_TOKEN used in step-level env: (not a GitHub environment) — zizmor requires job-level environment.
  # claude.yml / claude-code-review.yml: CLAUDE_CODE_OAUTH_TOKEN passed as secrets: to reusable workflow — cannot be in env:.
  # system-tests.yml: IO_JFROG_SBB_POLARION_TOKEN in job-level env: (not a GitHub environment) — needed by mvn/docker login.
  # Fork-PR workflows are additionally protected by GitHub's approval requirement.
  secrets-outside-env:
    ignore: [ci.yml, claude.yml, claude-code-review.yml, system-tests.yml]
  # Reusable workflows from our own org — tracking @main intentionally.
  unpinned-uses:
    config:
      policies:
        '*': hash-pin
        SchweizerischeBundesbahnen/*: any
